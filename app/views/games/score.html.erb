<h1>Results</h1>

<div>
    <% if @included %>
      <% if @english_word %>
        <p>"Congratulations! <strong><%= @guess %></strong> is a valid English word."</p>
      <% else %>
        <p>"Sorry but <strong><%= @guess %></strong> does not seem to be a valid English word..."</p>
      <% end %>
    <% else %>
      <p>"Sorry but <strong><%= @guess %></strong> can't be built out of <%= @letters %>"</p>
    <% end %>
</div>

<%= link_to 'Play again', 'new'  %>
<%= hidden_field_tag :score, @total_score %>

<h2><%= @total_score %></h2>
